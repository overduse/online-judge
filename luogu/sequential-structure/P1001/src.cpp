#include <iostream>
#include <cstdio>

using namespace std;

int main(){

    int x, y;
    // C Version Solution
    // scanf("%d%d", &x, &y);
    // printf("%d", x+y);

    // Cpp Version Solution
    cin >> x >> y;
    cout << x + y;

    return 0;
}
